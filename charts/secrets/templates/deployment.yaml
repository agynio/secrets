apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "secrets.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "secrets.name" . }}
    helm.sh/chart: {{ include "secrets.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "secrets.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "secrets.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        {{- with .Values.podLabels }}
{{ toYaml . | indent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
{{ toYaml . | indent 8 }}
      {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml . | indent 8 }}
      {{- end }}
      serviceAccountName: {{ include "secrets.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
{{ toYaml . | indent 8 }}
      {{- end }}
      containers:
        - name: secrets
          {{- with .Values.securityContext }}
          securityContext:
{{ toYaml . | indent 12 }}
          {{- end }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: 50051
              protocol: TCP
          {{- $db := .Values.database -}}
          {{- $url := "" -}}
          {{- $secretName := "" -}}
          {{- $secretKey := "database-url" -}}
          {{- if $db }}
            {{- with $db.url }}
              {{- $url = . -}}
            {{- end }}
            {{- with $db.existingSecret }}
              {{- with .name }}
                {{- $secretName = . -}}
              {{- end }}
              {{- with .key }}
                {{- $secretKey = . -}}
              {{- end }}
            {{- end }}
          {{- end }}
          {{- if and $secretName $url }}
            {{- fail "set only one of database.url or database.existingSecret.name" -}}
          {{- end }}
          env:
            - name: DATABASE_URL
              {{- if $secretName }}
              valueFrom:
                secretKeyRef:
                  name: {{ $secretName }}
                  key: {{ $secretKey }}
              {{- else }}
              value: {{ required "database.url or database.existingSecret.name must be set" $url | quote }}
              {{- end }}
            {{- with .Values.egressRules.grpcTarget }}
            - name: EGRESS_RULES_GRPC_TARGET
              value: {{ . | quote }}
            {{- end }}
{{- include "secrets.encryptionKeyEnv" . | nindent 12 }}
          livenessProbe:
            tcpSocket:
              port: grpc
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: grpc
            initialDelaySeconds: 5
            periodSeconds: 10
          {{- with .Values.resources }}
          resources:
{{ toYaml . | indent 12 }}
          {{- end }}
          {{- if .Values.secrets.encryptionKeyFile }}
          volumeMounts:
            - name: encryption-key
              mountPath: {{ dir .Values.secrets.encryptionKeyFile }}
              readOnly: true
          {{- end }}
      {{- if .Values.secrets.encryptionKeyFile }}
      volumes:
        - name: encryption-key
          secret:
            secretName: {{ .Values.secrets.encryptionKeySecretName | default "secrets-encryption-key" }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
      {{- end }}
