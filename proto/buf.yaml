version: v2

# Vendored until the ImagePullSecret removal lands in buf.build/agynio/api.
# Delete this directory and the `buf generate proto` step once it is published.
modules:
  - path: .

lint:
  use:
    - STANDARD
  except:
    - RPC_REQUEST_RESPONSE_UNIQUE
    - SERVICE_SUFFIX
